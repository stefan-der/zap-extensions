<zapaddon>
	<name>.DS_Store Spider</name>
	<version>1</version>			<!-- Integer that increments with each (released) change -->
	<status>alpha</status>			<!-- alpha | beta | release -->
	<semver></semver>			<!-- Optional, semantic version of the add-on -->
	<description>This Plugin tries to enumerate and display Files which are listed in directory Store Files</description>
	<author>stefan-der</author>
	<url/>						<!-- Optional URL to more info, eg on the zap-extensions wiki -->
	<changes/>					<!-- Optional, text summary of changes since the last version -->
	<dependencies>					<!-- Optional, dependencies of the add-on -->
		<javaversion></javaversion> <!-- Optional, minimum Java version (major.min) (e.g. "1.7", "1.8", "1.9") -->
		<addons/> <!-- TODO: commons.io hinzufügen -->
	</dependencies>
	<!--<bundle prefix="example">com.dsstore.Messages</bundle>--> <!-- Optional, declares a resource bundle to be automatically added/removed by core. The prefix attribute is used to add the bundle to Constant.messages. -->
	<helpset/>
	<!--<helpset localetoken="%LC%">com.dsstore.help%LC%.helpset</helpset>-->  <!-- Optional, declares a helpset to be automatically added/removed by core. The locale token is used to indicate where in the base name the locale might be present. -->
	<extensions>				<!-- Optional, to be used if this addon includes extensions -->
		<extension>org.zaproxy.zap.extension.spiderDSStore.ExtensionDSStoreSpider</extension>			<!-- Full class name of the extension - can be multiple of these -->
	</extensions>
	<ascanrules>				<!-- Optional, to be used if this addon includes active scan rules -->
		<ascanrule/>			<!-- Full class name of the active scan rule - can be multiple of these -->
	</ascanrules>
	<pscanrules>				<!-- Optional, to be used if this addon includes passive scan rules -->
		<pscanrule/>			<!-- Full class name of the passive scan rule - can be multiple of these -->
	</pscanrules>
	<files>						<!-- Optional, to be used if this addon includes raw files -->
		<file/>					<!-- Relative file name (to this package, and the users directory) - can be multiple of these -->
	</files>
	<not-before-version>2.7.0</not-before-version>		<!-- Optional, to be used if this addon can only be run from the specified version e.g. 2.1.0 -->
	<not-from-version/>			<!-- Optional, to be used if this addon can not run from the specified version e.g. 2.4.0 -->
	<author>stefan-der</author>
</zapaddon>
